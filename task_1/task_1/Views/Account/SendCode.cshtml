@model task_1.Models.SendCodeViewModel
@{
    ViewBag.Title = Resources.Resource.Send;
}

<h2>@ViewBag.Title.</h2>

@using (Html.BeginForm("SendCode", "Account", new { ReturnUrl = Model.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" })) {
    @Html.AntiForgeryToken()
    @Html.Hidden("rememberMe", @Model.RememberMe)
    <h4>@Resources.Resource.SendCheckCode</h4>
    <hr />
    <div class="row">
        <div class="col-md-8">
            @Resources.Resource.TwoFactorAutentificationChoose
            @Html.DropDownListFor(model => model.SelectedProvider, Model.Providers)
            <input type="submit" value="@Resources.Resource.Send" class="btn btn-default" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
